---
import Layout from "./Layout.astro";
import Hero from "../components/Hero.astro";

const { title, backLinkTitle, displayBacklink } = Astro.props;
---

<Layout title={title}>
	<Hero
		title={title}
		displayBacklink={displayBacklink}
		backLinkTitle={backLinkTitle}
		slot="after-header"
	/>
	<slot />
</Layout>
